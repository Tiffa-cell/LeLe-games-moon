/* params.js — 可调参数（颜色除外；颜色一律在 theme.js）
 *
 * 坐标单位是「画布单位」：以 M0 概念稿的画布为参考（宽 1668 × 高 2388）。
 * 屏幕比例不同时，画布会按比例加长或加宽（见 scene.js），构图数值不必改。
 */
window.PARAMS = {
  ref: { width: 1668, height: 2388 },   // 参考画布（M0）

  moon: {
    radius: 205,          // 主月亮半径
    yRatio: 0.356,        // 主月亮中心离画布顶部的比例（M0：850 / 2388）
    glowExtra: 190,       // 月晕半径 = 月亮半径 + glowExtra
    glowAlpha: 0.22,      // 月晕最亮时（满月）的不透明度
    glowMinRatio: 0.35,   // 新月时月晕保留的相对亮度
    rimAlpha: 0.28,       // 新月：暗盘上那圈细亮边的不透明度
    craters: [[-62, -42, 36], [52, 34, 23], [-12, 74, 16], [72, -72, 14]],  // [dx, dy, r]
    craterAlpha: 0.55
  },

  ring: {
    radius: 470,          // 相位环半径
    markerRadius: 46,     // 八枚固定小月亮的半径
    travelerRadius: 50,   // 可拖动的那颗小月亮的半径
    haloExtra: 46,        // 它的光晕外扩多少
    haloAlpha: 0.32,      // 光晕最亮处不透明度
    width: 3,             // 环的线宽
    dash: '1 26',         // 环的虚线样式（点、间隔）
    alpha: 0.28,          // 环的不透明度
    trailAlpha: 0.55,     // 本圈已走过弧线的不透明度
    grabBand: 130,        // 手指离环多远以内算「抓住了环」
    centerDeadZone: 90,   // 手指离环心太近时忽略（角度会抖）
    snap: true,           // 松手时吸附到最近的相位
    snapMs: 240
  },

  stars: {
    count: 46,            // 参考画布上的星点数（按画布面积缩放）；分成 8 批，经过第 i 个相位节点点亮第 i 批
    sparkles: 6,          // 四角星数：第一次通关时出现，之后一直留着（通关印记）
    dim: 0.2,             // 未点亮时星星的亮度系数（1 = 概念稿亮度）
    litBoost: 1.25,       // 点亮后的亮度系数（超过 1 的部分自动截到全亮）
    litScale: 1.2,        // 点亮后星星放大的倍数
    lightMs: 600,         // 每颗星从暗到亮的缓动时长（点亮时先闪到峰值，再落到常亮）
    igniteSettleMs: 300,  // 闪到峰值后落回常亮的时长
    igniteScale: 1.8,     // 点亮瞬间放大的倍数
    batchStaggerMs: 90,   // 同一批星星逐颗点亮的间隔
    peakScale: 2.2,       // 闪烁波峰值时星星放大的倍数（峰值颜色见 theme.js 的 starPeak）
    waveSpreadMs: 850,    // 闪烁波从最近的星扩散到最远那颗星的时间
    waveFlashMs: 700,     // 每颗星闪一下的时长（扩散 + 闪 ≈ 1.5 秒）
    resetMs: 1600,        // 镜头拉回时星空回暗的时长（下一圈可以再点亮一次）
    seed: 42              // 星空随机种子（同一屏幕比例下星空固定不变）
  },

  sun: {                  // 第2章：可拖动的太阳
    radius: 56,           // 太阳的基准半径（在轨道正中、左右两端时的大小）
    backScale: 0.6,       // 太阳在月亮正后方（椭圆最上端）时的缩放
    frontScale: 1.3,      // 太阳在月亮正前方（椭圆最下端）时的缩放；中间按前后深度连续过渡
    glowExtra: 120,       // 太阳光晕外扩多少
    glowAlpha: 0.42,      // 光晕最亮处不透明度
    grabRadius: 150,      // 手指离太阳中心多远以内算「抓住了太阳」（按太阳当前缩放）
    startAngle: 0,        // 进入第2章时太阳的轨道角（弧度，0 = 正右方 → 右半亮的弦月；-π/2 = 正后方；π/2 = 正前方）
    shadeOffset: 0.5,     // 球体感：亮面渐变的中心朝太阳方向偏移多少（× 月亮半径）
    shadeRadius: 1.5      // 球体感：亮面渐变的半径（× 月亮半径）
  },

  orbit: {                // 第2章：太阳的椭圆轨道（带俯角：横向长、纵向扁，像从稍高处俯视一个水平圆环）
    rx: 560,              // 椭圆横向半径
    ry: 215,              // 椭圆纵向半径（比月亮半径大一点：轨道线本身不穿过月亮，只有太阳会被月亮挡住 / 盖住月亮）
    moonYRatio: 0.44,     // 主月亮中心离画布顶部的比例（第2章没有地面，月亮可以更居中）
    width: 3,             // 轨道虚线的线宽 / 样式
    dash: '1 26',
    alpha: 0.34,          // 前半段（月亮前面）的不透明度
    backAlpha: 0.18,      // 后半段（月亮后面）的不透明度：更淡，有远近
    grabBand: 130,        // 手指离轨道多远以内算「抓住了轨道」
    deadZone: 0.3         // 手指离月心太近时忽略（按椭圆归一化的距离，1 = 在轨道上）
  },

  guide: {                // 第2章：三步引导（提词见 chapter2.js 的 STEPS）
    toleranceDeg: 20,     // 正前方 / 正后方目标允许的角度误差（±）：四岁孩子的手不精确
    gapMin: 0.25,         // 第二步：月亮缺掉的部分至少占多少算「明显缺口」
    holdMs: 1000,         // 在目标区域里停多久算完成（月亮外围的细弧线在这段时间里填满）
    arcGap: 34,           // 细弧线离月亮边缘多远
    arcWidth: 4,          // 细弧线的线宽
    stepDelayMs: 700,     // 一步完成后多久换下一句提词
    promptFadeMs: 350,    // 提词淡出 / 淡入的时长
    winWaveMs: 1600,      // 过关：闪烁波过去多久后出现返回键
    strip: {              // 屏幕下方边缘的 8 相位小月相带
      radius: 13,         // 小月亮半径（CSS px，在 40 px 的格子里）
      cell: 40
    }
  },

  month: {                // 第2章「一个月」回合（三步引导之后；引导只在首次出现，之后直接从这里开始）
    prompt: '下一个是这样的月亮',        // 八个目标共用的一句提词（目标月相画在左上角）
    tonightPrompt: '今晚的月亮，是什么样的？', // 最后一题：按设备日期算真实月相
    ring: {               // 屏幕上方一圈 8 颗空心小星（替代三个进度点），达成一个点亮一颗
      radius: 32,         // 圈的半径（CSS px）
      star: 9             // 小星的外接半径（CSS px）
    },
    target: {             // 左上角的目标月相
      radius: 26          // 小月亮半径（CSS px）
    },
    swapMs: 350           // 换目标：淡出 / 淡入的时长
  },

  camera: {
    introHoldMs: 900,     // 开场停在小人身上多久
    introZoomMs: 2600,    // 开场从小人拉远到宇宙视角的时长
    winDelayMs: 1500,     // 走满一圈后，等闪烁波过去多久镜头再落到小人身上
    winZoomMs: 2400,      // 镜头落到小人的时长
    holdMs: 2000,         // 停在小人身上多久后缓缓拉回（点一下屏幕可以提前）
    returnZoomMs: 2400,   // 从小人缓缓拉回原构图的时长
    switchZoomMs: 1500,   // 换章节时：镜头先落到小人身上的时长（然后在小人身上换场景，再从他身上拉远）
    kidFrameRatio: 0.32   // 小人镜头的宽度 = 画布宽 × 此比例
  },

  kid: {
    lookUpMs: 700         // 小人抬头的时长
  },

  win: {
    lapEpsilon: 0.02      // 判定「回到起点」的容差（弧度）
  }
};
