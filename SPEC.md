# 月相 — 项目规格 v1.0

## 定位
给一个四岁孩子做的月相科普互动应用。一次只讲一个主题，
安静、极简、无文字依赖（可有极少量图标，不依赖阅读）。
风格参照：纪念碑谷。美术基调已由 M0 概念稿定稿（见 assets/ 中黛蓝版）。

## 灵魂（所有章节必须遵守）
主线是「我们是地球的一部分」。M0 稿中右侧山丘上那个看月亮的小人是玩家的化身。
小人只出现在有视角切换（拉远 / 拉回）的章节：这些章节做一次
「小人视角 → 拉远到宇宙视角 → 回到小人视角」的往返，结尾镜头回到他身上。
第2章是玩家自己的眼睛——画面就是孩子抬头看到的那一片天，不出现小人和房子。

## 技术铁律
1. 网页应用：单个 index.html（可拆 css/js，但零依赖、零构建、双击即开）。
   允许每章出现少量中文引导语，定位是「给念的人的提词」：孩子不需要识字也能玩；
   字号大，每次只显示一句。
2. iPad 竖屏优先，Safari 可用，支持触摸拖拽；加到主屏后全屏运行。
3. 所有颜色集中在 theme.js 一处（天空上端色、天空下端色、月亮色、
   月晕色、剪影色、暖窗色、相位环虚线色、文字色），程序任何地方不许写死颜色。
   预置三套夜色：ink（墨青）、indigo（黛蓝，默认）、violet（暗紫），可切换。
4. 全部矢量或代码绘制（SVG/Canvas），不使用位图贴图；天空、星、相位环、地面剪影分层。
5. 无音效、无弹窗、无计时压力、无分数。过关反馈只用画面变化（例如星星轻轻亮起）。
6. 章节进度保存在 localStorage，但清空也无所谓，不做账户。

## 章节
### 第1章 圆缺循环（现象）
目标：月亮不是变大变小，是循环变化，一圈一圈。
画面：M0 稿的构图——中央大月亮，环绕一圈八个相位小月亮，新月位为空心圆；地面小屋、小人。
交互：拖动相位环上的一颗小月亮沿环滑动（或直接拖环），中央大月亮的相位实时跟随。
过关：拖满完整一圈（回到起点）。过关时小人抬头，天空星星逐颗点亮。

### 第2章 月亮不发光
目标：月亮亮的那一面永远朝着太阳。
视角：玩家自己的眼睛。不出现小人和房子，没有镜头拉远 / 拉回。
画面：只有夜空、中央大月亮、太阳、太阳轨道。去掉地面、房子、小人。
轨道：一个带俯角的椭圆（横向长、纵向扁，类似从稍高处俯视一个水平圆环）。太阳沿椭圆拖动：
走到椭圆上半段时表示在月亮后方——太阳变小（约 0.6 倍）、位于月亮上方、被月亮遮挡；
走到下半段时表示在月亮前方——太阳变大（约 1.3 倍）、位于月亮下方、在月亮前面。
月亮的明暗用 SVG 遮罩实时跟随太阳方向，亮面永远朝太阳；太阳在正后方时新月，正前方时满月。
引导流程（三步，每步屏幕下方一句中文提词，字号大，一次只显示一句）：
- 第一步 提词「你能找到满月吗？」目标：把太阳拖到正前方。
- 第二步 提词「满月是从哪一边开始变缺的？」目标：从满月位置把太阳往任意一边拖，
  直到月亮出现明显缺口（≥ 25%），停住即算。
- 第三步 提词「能把月亮藏起来吗？」目标：把太阳拖到正后方（新月）。
达成判定：太阳进入目标区域（角度误差 ±20°）后开始计时，在中央月亮外围画一圈逐渐填满的细弧线，
填满（1 秒）即算完成；中途拖出区域弧线清空。
进度显示：屏幕上方三个小圆点，完成一步点亮一个。三个全亮即过关。
过关反馈：星星波（沿用第1章），然后出现「←」返回键（呼吸动画），不再有镜头回小人的动作。
辅助：屏幕下方边缘放一条 8 相位的小月相带，当前相位高亮，帮助念的人对照。
导航：第1章过关后的太阳入口右侧加「→」，第2章的返回键左侧加「←」，都带呼吸缩放动画。
所有新颜色（太阳色、轨道色、提词字色、进度点色）进 theme.js。
回合（M2.2）：三步引导完成后不直接过关，进入「一个月」模式——上方的三个进度点换成一圈 8 个空心小星；
目标依次为新月、蛾眉月、上弦月、盈凸月、满月、亏凸月、下弦月、残月，提词只显示一句「下一个是这样的月亮」并配左上目标月相；
每达成一个（同样的 ±20° 停 1 秒判定）点亮一颗星。8 颗全亮后出最后一题「今晚的月亮，是什么样的？」，
目标月相用设备当前日期计算真实月相（简单朔望月算法，误差一天内），达成后才触发过关反馈。
过关后第2章可以重进，直接从「一个月」模式开始；三步引导只在首次出现。

### 第3章A 绕圈观察
目标：月亮绕着地球转，太阳在远处照着。
画面：上帝视角——太阳在一侧，地球居中，月亮绕地球轨道运行。
交互：拖动月亮沿轨道走，或按住让它自动转。
过关：观察满一圈。

### 第3章B 双视角
目标：上帝视角的位置 = 小人抬头看到的形状，是同一件事。
画面：主画面沿用 3A；右下角开一个角窗，显示地面小人抬头看到的月亮。
交互：拖动轨道上的月亮，角窗里的月相同步变化。
过关：按角窗里出现的目标相位，在主画面把月亮拖到正确位置（连续三次）。

### 第4章 每天晚50分钟
目标：每天同一时刻看，月亮在天上的位置一天天往后挪。
画面：地面视角，小人和他的天空；一个「过一天」按钮。
交互：每按一次过一天，月亮的位置和相位一起变化，留下淡淡的前几天的残影。
过关：过完一个月。

### 番外 潮汐锁定
目标：月亮永远同一面朝着地球。
画面：上帝视角，月球表面画一个明显的记号（一张脸）。
交互：拖月亮绕地球转，记号始终朝地球。
过关：转满一圈。

### 通关 三球仪
自由模式：太阳-地球-月亮全部可拖，角窗常开，可切换夜色。无目标。

## 交付规则
每个里程碑结束：合并到 main，README 里写清如何在 iPad Safari 打开，
并把本次新增的可调参数列出来。
